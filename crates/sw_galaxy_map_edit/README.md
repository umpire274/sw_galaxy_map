# sw_galaxy_map_edit

Command-line editor for maintaining planet records in the local `sw_galaxy_map` SQLite database.

## Status

Early scaffold for the `v0.1.0` editing tool.

## Planned features

- find planet records by name, alias, or FID
- interactive guided editing
- field validation
- change preview
- audit trail for manual edits